/**
 * Legal Policy header
 *
 * Used at the top of every legal policy post
 *
 * @property {string} title
 * @property {string} date
 */
export interface LegalPolicy {
    title: string
    date: string
}